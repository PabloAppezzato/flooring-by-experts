import { Injectable } from '@angular/core'; import { CanActivate, ActivatedRouteSnapshot, Router } from '@angular/router'; import { AuthService } from '../services/auth.service'; @Injectable({providedIn:'root'}) export class RoleGuard implements CanActivate { constructor(private auth:AuthService, private router:Router){} canActivate(route:ActivatedRouteSnapshot){ const expected=route.data['roles'] as string[]; const role=this.auth.role; if(!role || !expected.includes(role)){ this.router.navigate(['/']); return false; } return true; } }
